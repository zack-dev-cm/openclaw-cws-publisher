interface:
  display_name: "OpenClaw CWS Publisher"
  short_description: "Package and publish Chrome extensions with OpenClaw, GitHub, ClawHub, and leak checks"
  default_prompt: "Use $openclaw-cws-publisher to inventory local Chrome extensions, build the new extension ZIP and store assets, scan for publish leaks, generate launch metadata, and drive Chrome Web Store submission through OpenClaw browser automation."
